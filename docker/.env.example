# Backend API Keys (keep these secret!)
LOGAINM_API_KEY=your_logainm_api_key_here
GEOGRAPH_API_KEY=your_geograph_api_key_here
GRAPHHOPPER_API_KEY=your_graphhopper_api_key_here

# Frontend URL for CORS (set to your domain in production)
# Examples: https://learscail.ie, http://localhost
FRONTEND_URL=http://localhost

# Note: VITE_BACKEND_URL is now baked into the Docker images at build time
# The published images use relative URLs for nginx proxy setup
